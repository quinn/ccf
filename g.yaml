version: "1"
include:
  g: "gh:quinn/g"
generators:
  - name: init
    args:
      - mod
    pre:
      - which task
    post:
      - go mod init {{ .mod }}
      - task gen-pages
      - go mod tidy
      - go get github.com/a-h/templ
  - name: page
    args:
      - path
